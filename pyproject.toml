[project]
name = "flutter-build-it"
version = "0.1.5"
description = "Flutter multi-flavor build automation CLI — reads flavorizr config and builds all your flavors in one command."
authors = [
    { name = "Dayane S. R. Assogba", email = "assogbadayane197@gmail.com" },
]
requires-python = ">=3.10"
readme = "README.md"
license = "MIT"
keywords = ["flutter", "flavorizr", "build", "cli", "dart", "mobile"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Build Tools",
]
dependencies = [
    "typer>=0.24.1",
    "rich>=14.3.3",
    "pyyaml>=6.0.3",
    "pydantic>=2.12.5",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.0.3",
    "pytest-asyncio>=1.3.0",
    "auto-py-to-exe>=2.50.0",
    "pyinstaller>=6.6.0",
    "build>=1.2.0",
    "twine>=5.0.0",
]

[project.urls]
Homepage = "https://github.com/dasero197/build_it"
Repository = "https://github.com/dasero197/build_it"
Issues = "https://github.com/dasero197/build_it/issues"

[project.scripts]
build_it = "build_it.cli.main:app"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv]
package = true

[tool.setuptools.packages.find]
where = ["."]
include = ["build_it*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
